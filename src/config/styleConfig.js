// General
const MOBILE_MEDIA_QUERY = '@media(max-width: 768px)';
const devBorder = (color='black') => 'border: dotted thin ' + color + ';';

// Navigation
const NAV_BAR_HEIGHT = '50px';

export default {
    MOBILE_MEDIA_QUERY,
    devBorder,

    NAV_BAR_HEIGHT
};
